# [Unreleased]

## 待提交改动

## 提交记录

## 完善 SAM 标注与 AI 预标注交互

- 完善 AI 预标注顶部布局与模型显示：普通 YOLO 保留置信度和 IoU，SAM 3 隐藏无关控件并将高级参数开关移到标注形状右侧，模型下拉框仅显示 `sam3.pt` 文件名。
- 优化 SAM 3 类别映射表和文本提示词输入框的行高、内边距与文字对齐，避免小窗口下内容裁切。
- 增强 SAM 高级设置：支持选择当前模型、打开模型目录并在保存后同步模型，最小掩码面积提供对数滑块与精确数值输入，数值控件移除易遮挡内容的上下调按钮。
- 收紧轮廓简化比例范围至 `0.00%~1.50%` 并保持滑块、数值框和运行时校验一致，修复 Windows 下数值框布局遮挡问题。
- 调整“画标注框”窗口尺寸、间距、模型下拉框压缩策略和“高级”按钮宽度，保持 SAM 模型选择与高级设置联动。

## 完善环境打包与 SAM 标注能力并修复安装器复用（c671ab4）

- 扩展画标注框窗口的 SAM 模型目录：识别所有 `sam` 前缀 `.pt`，为 SAM 1、SAM 2/2.1 和 SAM 3 提供版本/架构简名，保留用户自定义名称，并接入 SAM 3 CUDA 画布点提示辅助标注。
- 扩展数据标注页 SAM 画布辅助能力：模型下拉框新增官方 `sam3.pt` CUDA 单点分割，增加项目级现代化高级设置窗口，支持多候选优选、最低预测质量、最小掩码面积和轮廓简化，并在 AI 预标注启动前释放画布模型显存。
- 修复完整 Windows 发布将完整冻结 EXE 重复打入程序安装器的问题：基础环境构建完成后强制重建 program-only 程序 staging，并补充安装器体积与构建顺序回归契约。
- 调整环境包识别策略：安装器按基础包/附加包的版本化文件名识别，不再绑定某次压缩产物的大小和 SHA-256，允许同版本环境包重打包后继续复用；保留包内 manifest 与运行时协议检查。
- 移除基础环境与附加环境的文件哈希计算和验证流程；环境清单改为登记文件路径，继续保留版本、协议、路径安全、文件集合和 7-Zip CRC 解压检查。
- 收紧 GitHub 提交流程约束：明确 CHANGELOG 时间格式、提交正文一致性、低价值总结过滤和版本归档例外。

# [1.3.2] - 2026-07-29

YOLOTool 1.3.2 新增数据标注页的 SAM 2/2.1 智能辅助标注与 Meta SAM 3 文本分割预标注，完善标注画布的实时预览、运行时控制及项目级设置。基础运行环境升级为 v3 并携带 SAM 3 推理依赖，用户仍需自行放置官方模型权重。

## 本版本提交记录

## 新增 SAM 智能辅助标注与文本分割预标注（2ffe05e）

- 新增数据标注页 SAM 2/2.1 智能辅助标注，支持矩形、有向矩形、镜像有向矩形和多边形的实时分割预览与确认；模型选择及动画开关按项目保存。
- 新增兼容官方 Meta `sam3.pt` 的 AI 文本分割预标注，支持按项目类别填写提示词、跨类别 mask 去重、面积过滤、Labelme/YOLO 同步和整轮撤销；SAM 3 仅支持 CUDA，权重由用户自行放入 `data/models/`。
- 基础运行环境升级为 v3，加入固定提交的 SAM 3 Windows 推理 vendor wheel、SAM License 与 NumPy 2.x 兼容依赖，但不打包 `sam3.pt`，运行时协议保持 `runtime-2`。
- 增加模型发现、mask 几何转换、后台运行时及并发控制，完善画布交互与生命周期释放；同步更新服务/UI/竞态回归测试及标注、架构、打包和代码清单文档。

# [1.3.1] - 2026-07-28

YOLOTool 1.3.1 未新增 YOLO 训练、标注、验证和模型格式转换等核心业务功能，主要聚焦项目结构、运行时边界与 Windows 分层打包体系的优化。版本增加 SAM2 相关依赖、代码和基础运行环境，为后续 SAM2 自动标注能力准备基础；同时完善发布清单、运行时探测、程序与环境包更新机制，为软件内置检测与更新能力做好基础建设。

## 本版本提交记录

## 1.3.1 版本补充改动与改动总结（099409b）

- 完善安装器完成页清理选项、Restart Manager 进程处理、首次安装基础环境强制规则和安装后兼容性警告。
- 修复附加环境跨盘迁移、安装清单和版本读取，按 Release 资源版本准确判断环境更新。
- 完善更新窗口的基础/附加包提示、重装替换确认与三项资源组合逻辑。
- 同步更新 README、规格、架构、打包文档、代码清单和回归测试。

## 完善 Release 更新检查与安装体验（d100e82）

- 新增 GitHub Release 检查、版本提示、更新窗口和多资源下载流程。
- 支持真实 Downloads 路径、暂停继续、附加环境热安装及安装器启动失败恢复。
- 放宽缺少基础环境时的程序更新限制，补充安装器清理、GitHub 下载入口和冻结清单。
- 同步更新设置、架构、打包、README、代码清单与服务/UI/集成回归测试。

## 完善 Windows 打包环境与发布流程（1be5bc4）

- 升级基础包与附加包到 v2，集成 SAM2 并拆分模型转换后端。
- 精简基础 staging，统一 7-Zip mx=5、多线程、实时进度和耗时提示。
- 移除环境包缓存文件与缓存判断，更新安装器、Pixi 配置、文档和测试。

## 完成架构整改、打包流程与文档同步（214be63）

- 将 TensorRT 附加环境迁移到当前安装实例并兼容旧版扩展。
- 统一 AppSettings、WorkbenchContext、任务协调器和 CLI 分发，删除旧兼容层并补充结构围栏。
- 增加 Windows 运行包输入指纹缓存，清理冗余打包入口与空 hook。
- 分层测试任务并迁移 pytest 缓存，更新安装器、规格、架构、README 和代码清单。
- 同步程序与安装器版本发布适配及完整打包要求。

# [1.3.0] - 2026-07-26

YOLOTool 1.3.0 完成了发布体系与模型转换链路两项重大改造：一方面重构 Windows 发布物和安装事务，将程序更新与大型运行环境解耦；另一方面新增模型格式转换服务和页面，为 `.pt` 模型导出多种部署格式，并通过独立附加运行时扩展 TensorRT 能力。

### 重构程序安装流程

- 重构发布产物为 `YOLOTool_Setup_<版本>.exe`、`YOLOTool_BaseEnv_v1.7z` 和 `YOLOTool_ExtraEnv_v1.7z` 三个层次：程序包负责 `YOLOTool.exe`、界面资源、安装逻辑和程序清单；基础包负责 `_internal`、ONNX/CPU 运行库、Torch、Ultralytics、OpenCV、CUDA 和内置模型；附加包只补充基础包缺少的 TensorRT 运行库。
- 实现程序层与环境层分离，程序安装包不再携带大型 `_internal` 和模型。基础环境版本未变化时，程序功能更新只需重新构建和分发小型程序安装包。
- 重构安装器首次安装流程：自动扫描安装器同目录的基础包，也支持在组件页手动选择；首次安装在缺少合法基础包时阻止提交，确保程序、基础环境和模型完整配套。
- 实现已安装实例的程序-only 升级、基础环境主动重装、多版本并行安装和修复安装；默认升级不重复替换基础环境，主动选择基础环境组件时才执行环境和官方模型替换。
- 将 TensorRT 运行时设计为独立可选组件，不阻塞程序和基础环境安装；用户可在安装器组件页选择附加包，也可在软件运行后从“模型格式转换”或“系统设置”页面导入。

### 新增模型格式转换与附加运行时

- 新增数据处理页“模型格式转换”和独立 `model_export` 服务域，实现 `.pt` 到 ONNX、TorchScript、OpenVINO、TensorRT、NCNN 的格式映射、命令构建、能力探测、临时输出、成功替换、停止清理和结构化日志。
- 完善转换页面的模型扫描、输出目录、输入尺寸、ONNX 简化、预览、覆盖确认、实时日志和停止操作，并将格式能力与运行时状态从 UI 中解耦到服务层。
- 将 ONNX、TorchScript、OpenVINO 和 NCNN 纳入基础环境；TensorRT 转换改为依赖 `YOLOTool_ExtraEnv_v1.7z`，缺少附加包时明确提示，且继续受 NVIDIA GPU、驱动和 TensorRT 版本约束。
- 实现附加包在“模型格式转换”和“系统设置”页面的文件浏览与拖放导入，增加已有版本替换确认、包校验、安装进度、原子切换和失败回滚，避免通过网络下载运行时依赖。

## 本版本提交记录

## 新增模型格式转换与分层安装发布（2735696）

### 模型格式转换与运行时能力

- 新增数据处理页“模型格式转换”二级页面，支持 `.pt` 导出 ONNX、TorchScript、OpenVINO、TensorRT 和 NCNN，并提供模型扫描、输出目录、输入尺寸、ONNX 简化、预览、覆盖确认、停止和实时日志。
- 新增独立 `model_export` 服务域，统一格式映射、能力探测、命令构建、临时输出、成功替换、停止清理和结构化 CLI 事件；保留 `YOLOTool.exe --yolo-export` 入口兼容性。
- 支持模型转换附加环境包的拖放识别、快速检查、安装/替换确认、SHA-256 校验、原子切换、旧版本保留和失败回滚；TensorRT 缺失或硬件不可用时显示明确状态。
- 根依赖加入 ONNX/ONNXSlim，补充 `release-base` 与 `export-full` Pixi 环境，明确区分 CPU `onnxruntime` 和 GPU `onnxruntime-gpu`，并覆盖 OpenVINO、NCNN、PNNX 与 TensorRT 的发布构建规则。

### Windows 分层发布与安装

- 将发布物拆分为小型 `YOLOTool_Setup_<版本>.exe`、`YOLOTool_BaseEnv_v1.7z` 和可选 `YOLOTool_ExtraEnv_v1.7z`，程序更新不再重复打包大型基础环境。
- 安装器新增同目录/手动选择的 companion catalog、组件识别、多实例安装路径、版本升级/修复/降级提示、基础环境强制规则和附加环境可选规则。
- 增加 staging、空间检查、SHA-256 校验、原子替换、备份回滚、官方模型合并更新、实例清单和冻结版启动探测；元数据集中到 `_internal\yolotool_metadata`，并保留旧版本迁移兼容。
- 基础环境安装和升级会管理 `data/models/yolo26n.pt` 以及根目录兼容副本，卸载时删除受管文件并保留用户模型、项目设置、图片、标签和结果。
- 优化安装器组件页和附加包安装进度：大包哈希从页面加载阶段延后到安装阶段，附加包解压进度映射到 5%-95%，并修复退出时 `{app}` 未初始化、冻结版 Python DLL、`ctypes.util`、PySide6 外置运行时和进度显示问题。
- 提供“打包更新程序”和“完整打包”两个 Windows 入口，统一卸载文件命名为 `uninstall.exe/.dat`，将 app 资源编译进程序本体，并保持基础包与附加包版本为 `v1`。

### UI、设置与工程维护

- 系统设置环境检测调整为显示 ONNX 与 TensorRT 版本，移除不必要的 PySide6 检测项，并刷新附加环境安装后的状态。
- 模型格式转换菜单使用与侧栏一致的明亮多格式文件符号 `🗂️`，固定在数据处理工具列表底部。
- 同步更新架构、数据处理与系统设置规格、Windows 打包文档、README、代码清单、Pixi 锁文件和安装器契约测试。
- 完成服务、UI、集成、安装器契约和打包相关回归测试；`pixi run check` 通过，完整测试 `157 passed`。

# [1.2.7] - 2026-07-24

本版本聚焦标注画布 OBB 编辑能力、数据处理与主页显示体验，以及 Windows 分层发布维护：新增普通有向矩形旋转和镜像有向矩形优化编辑，完善 Labelme 兼容读写；修复全屏布局与训练曲线横轴，强化类别管理、数据集划分和主页统计；同步放宽结构围栏硬上限、更新程序版本至 `1.2.7`，并补充规格、架构、README、代码清单和回归测试。

## 本版本提交记录

## 完善 OBB 画布编辑与结构维护（a705321）

- 完善普通有向矩形旋转控制点和镜像有向矩形优化编辑，支持围绕中心旋转、中心线与对称宽度控制，并通过 Labelme 标记保持镜像形状和直线扩展的兼容读写。
- 修正主页训练曲线横轴，改用 `results.csv` 的实际 Epoch 轮次；修复数据处理页切换全屏后的内容区扩展，并补充布局回归测试。
- 放宽页面、画布、后台 worker 与服务模块的行数硬上限，保留原阈值作为职责审查建议线，更新结构测试、维护规则及代码清单。
- 同步更新设置默认值、README、架构和标注/数据处理规格；完整测试通过 `99 passed`。

## 完善标注类别管理与数据处理工作流（5794168）

- 完善数据标注页目标类型切换、项目类别管理、Labelme 类别扫描、类别删除保护和类别转换设置窗口，并同步刷新画布与控制点颜色。
- 将数据处理页“标注转换”改为“数据集划分”，从项目类别管理读取类别并将类别映射行号统一为从 0 开始。
- 修正主页训练历史与图表的间距、对齐和圆角边框显示。
- 精简并重组 pytest 核心回归测试，补充类别管理、数据处理、主页和入口相关覆盖；完整测试通过 `92 passed`。

## 完善主页统计与 Windows 分层发布流程（5c720c1）

- 重构主页图片与多类别标注分布统计，补充未标注和无类别兜底行为。
- 新增运行环境兼容性检查、发布清单和分层 Windows 程序/运行环境打包流程。
- 更新入口、设置、资源路径、发布脚本及相关 README、架构、规格和代码清单文档。

## 更新程序版本至 1.2.7

- 更新源码运行时 `APP_VERSION`、Windows Inno Setup 安装包版本和设置页版本回归断言。

# [1.2.6] - 2026-07-23

本版本聚焦模型验证交互、数据标注状态反馈、主页高 DPI 显示和 Windows 打包稳定性：完善图片、视频、摄像头与数据集验证源处理及预览，新增数据标注页底部状态栏，修复窗口图标与主页图表在高 DPI 缩放下的模糊问题，并优化主页图表间距、打包资源路径和模型复制规则。同步更新程序版本、规格文档、架构说明、README、代码清单和回归测试。

## 本版本提交记录

## 完善验证、标注与桌面显示体验（78cdd1b）

- 统一图片、视频、摄像头和数据集验证的检测入口与输入源处理，完善检测前预览、导航、结果保存和数据集临时验证路径。
- 新增数据标注页底部当前模式状态栏，优化状态栏布局、显示开关和右键菜单未完成绘制提示。
- 修复顶部导航图标、主页各类别图片分布和训练曲线在 Windows 高 DPI 缩放下的模糊问题，并调整主页图表坐标与环境标签显示。
- 修复 PyInstaller 冻结态图标路径，统一基础模型从 `data/models/*.pt` 复制到打包产物，移除重复模型文件。
- 更新程序运行时版本至 `1.2.6`，补充规格、架构、README、打包文档、代码清单和 UI 回归测试；完整测试通过 `220 passed`。

## 完善验证页检测模式与验证源交互（fdde964）

- 统一图片、视频、摄像头和数据集验证的主检测按钮文案为“开始检测”，恢复摄像头与数据集验证的开始/停止流程。
- 支持自定义文件夹与 `data.yaml` 临时验证路径，并兼容“摄像头检测”旧配置值。
- 修正检测前源计数、导航、布局边距和摄像头实时预览工具栏等交互。
- 补充验证服务/UI 回归测试，并同步更新规格、架构、README、打包说明和代码清单。

# [1.2.5] - 2026-07-22

本版本聚焦模型验证的图片与视频检测工作流：统一检测模式和输入源处理，增加检测前媒体预览与导航，完善视频批量检测、播放预览、结果保存和拖放输入，并修复预览布局、播放状态与检测源切换问题。同时迁移规格文档目录、修复代码清单扫描范围，补充验证服务/UI 回归测试，并将程序版本更新至 `1.2.5`。

## 本版本提交记录

## 完善验证页输入源与预览交互（4ae0c3c）

- 完善检测前源图片预览与导航，支持在检测前切换文件夹中的输入源，并在切换检测模式或输入源时清理旧媒体和结果。
- 支持图片/视频单个文件、文件夹和拖放输入，恢复 Windows 原生文件夹选择，并完善视频自然结束后的播放状态。
- 固定验证页源图与检测结果预览的等宽布局，移除重复模块标题，补充对应服务层/UI 回归测试。
- 修复代码清单扫描范围，迁移规格文档目录并同步更新维护文档。

## 完善模型验证图片与视频检测流程（6d98b89）

- 统一验证页检测模式为图片检测、视频检测、摄像头和数据集验证，并保留旧配置兼容；自定义输入源显示项目相对路径。
- 新增视频批量检测流程、每秒进度与速度日志、MP4 结果输出和视频播放预览，不再保存视频帧级 TXT 标注。
- 完善视频预览首帧显示、播放暂停、同步进度拖动、视频切换、结果列表、保存目录入口和等宽双视频面板，批量检测后续视频不覆盖当前预览。
- 优化验证页数据集模式左侧布局、普通检测日志高度和视频/图片模式切换，修复视频切换为图片时左侧栏短暂变窄闪动的问题。
- 补充验证服务/UI 回归测试，并同步更新 README、规格、架构、打包说明和代码清单。

# [1.2.4] - 2026-07-21

本版本聚焦项目路径配置一致性、数据标注工作流和系统设置体验：完善跨页面路径即时同步，优化标注页设置与快捷键，修复 AI 预标注类别映射滚动行为，并将系统设置与 Windows 安装程序版本统一更新为 `1.2.4`。同时补充相关回归测试、规格文档和架构说明。

## 本版本提交记录

## 完善项目路径同步与标注设置（ed30264）

- 调整 CHANGELOG 哈希回填规则：回填后不再单独提交或推送，改为随下一次正常提交同步；仅软件大版本更新时按版本归档流程重复提交。
- 完善标注页“更多设置”：恢复 YOLO 标注文件夹相对路径编辑项，默认关闭快捷标注，补充配置解释提示，并将窗口最终尺寸调整为 `300 x 420`。
- 修复 AI 预标注类别映射滚动行为，鼠标指向标注类别下拉框滚轮时继续滚动映射列表，不切换下拉选项。
- 完善项目路径跨页面即时同步，明确图片、Labelme、YOLO 目录共享关系，并将图片压缩源目录独立保存。

## 完善标注页设置与快捷键交互（a3285cb）

- 整理标注页更多设置：移除 YOLO 标注文件夹编辑项，使用项目 `labels_dir`，直线扩展改为复选框。
- 新增画布当前模式状态显示开关，统一设置窗口尺寸、按钮文案和可伸缩布局。
- 补齐 `W/V/R/O/M/P/C/L` 标注快捷键，并补充回归测试。
- 同步更新 README、规格、架构、代码清单、运行时默认配置和安装版本。

# [1.2.3] - 2026-07-20

本版本聚焦数据标注画布的交互一致性与稳定性：统一控制点在绘制、悬浮和选中状态下的显示规则，修复圆形标注半径点的预览方向与持久化位置，扩大编辑命中范围，并解决 Qt UI 测试退出时的原生崩溃问题。同时补充服务层与 UI 回归测试，更新相关规格、架构、README、代码清单和维护规则。

## 本版本提交记录

## 完善标注控制点交互并修复圆形半径点（b04aa2d）

- 完善标注控制点显示与编辑交互，统一实心/空心圆点与方块样式，扩大编辑命中范围。
- 修复圆形标注预览半径点方向、完成后固定位置及 Labelme JSON 半径点读写。
- 修复 Qt UI 延迟回调生命周期与测试退出时的 `0xC0000409` 原生崩溃。
- 补充服务层/UI 回归测试，并同步更新 AGENTS、README、规格、架构和代码清单。

## 完善标注画布控制点显示与编辑交互（7c5dd0d）

- 所有标注模式显示控制点，绘制模式仅展示不可交互。
- 编辑模式细化控制点悬浮形状，支持直接拖动命中的控制点，并统一悬浮与选中背景深度。
- 将控制点固定为直径 `9 px`，完善绿色预览、矩形框十字辅助线和相关回归测试。
- 更新程序版本至 `1.2.3`，并同步规格、架构、README 与代码清单。

## 修复标注交互并完善矩形框十字辅助线（0ad97b2）

- 修复关闭快捷标注时矩形框第二次点击完成后画布未及时刷新，并完善编辑、连续标注和绘制状态清理。
- 增加标注名称显示设置，统一控制点尺寸与绘制模式光标行为，并保持系统短十字光标显示。
- 为矩形框增加贯穿画布的纯黑实线十字辅助线，在系统短光标热点周围保留 `20px` 原始背景采样空隙，避免短光标被长线反转为浅色。
- 补充矩形框交互、光标、渲染与设置回归测试，并同步更新规格、架构、README 和代码清单。

# [1.2.2] - 2026-07-09

本版本聚焦于桌面工作台的可用性与一致性修复，重点完善了开发态与打包态的图标链路、标注页交互命名与入口组织，并继续优化首页与标注页的大目录首屏体验，同时补齐提交记录维护规则与相关文档说明。

## 本版本提交记录

## 修复图标链路并调整标注编辑入口文案（6abd1be）

- 修正 `CHANGELOG.md` 与 `AGENTS.md` 的提交记录规则：最新一次总提交不得在同一提交里回填自己的 hash，避免 amend 后 hash 反复变化的自引用问题；仅允许后续独立整理时回填已稳定存在的历史提交 hash。
- 修复 GUI 图标加载链路，统一改为通过 `src/shared/paths.py` 读取 `src/assets/app\\\_icon.png`，并在 `QApplication` 与主窗口两层同时设置图标，恢复开发态与打包态窗口/任务栏图标显示。
- 调整数据标注交互文案：主画布右键菜单的“选择”改名为“编辑”；“画标注框”弹窗顶部新增独立“编辑”入口，并与下方绘制类型分组分隔；同步更新标注规格与 README。

## 优化首页与标注页首屏性能并收紧标注转换输出规则（ef04c8f）

- 主窗口改为先显示首页、空闲后分批预热其余页面，主页概览统计/类别分布/训练曲线/训练历史改为后台汇总刷新，切回主页时保留旧统计值直至新结果返回，减少启动和切页卡顿与闪动。
- 标注页首轮图片扫描延后到页面首次显示后启动；预热阶段可提前准备首张图片与首批列表项，并继续后台分批补齐图片列表和标注状态，修复文件名重影与只读勾选框发灰问题，补充对应服务层与 UI 回归测试。
- 收紧标注转换输出目录创建规则：仅在实际写出新文件时创建目录，未开启备份时不再创建 `old/`，空 split 不再生成 `train/val/test` 目录，`data.yaml` 同步去掉空 split 条目。
- 图片压缩页新增“打开结果文件夹”，并同步更新 README、架构/规格文档、代码清单以及 AGENTS/CHANGELOG 提交流程约束说明。

## 修复开发态 CLI 根路径并预备 1.2.2 维护更新（0c3aee5）

- 同步更新 `docs/code-inventory.md`，并清理阶段性重构计划文件。
- 在项目根目录新增 `YOLOTool.lnk` 快捷方式，直接打开 `src/open\\\_yolo\\\_tool.pyw` 并复用项目程序图标。
- 修正 `CHANGELOG.md` 记录规则，改为“最新在最上”的纯记录格式，并同步约束 `AGENTS.md`。
- 修复开发态共享根路径解析错误，确保训练/验证等隐藏 CLI 子进程从仓库根目录启动，不再因 `python -m src.main` 找不到 `src` 包而直接退出。
