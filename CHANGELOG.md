# [Unreleased]

## 待提交改动

- 2026/07/20 00:07：完善数据标注画布控制点显示与编辑交互：所有模式显示角点，绘制模式不可交互；编辑模式仅悬浮点显示方块并可直接拖动，悬浮背景与选中背景深度一致。
- 2026/07/20 00:07：将数据标注画布控制点固定为直径 `9 px`，同步规格与回归测试。
- 2026/07/19 19:52：修复矩形框绘制中离开再进入画布后短十字光标和辅助线丢失的问题；同时保持十字自适应灰阶、编辑模式选中标注的常驻半透明背景，以及绿色预览与底图混合效果。
- 2026/07/19 00:00：将系统信息中的程序版本更新为 `1.2.3`，同步设置页规格与回归测试。

## 提交记录

## 修复标注交互并完善矩形框十字辅助线

- 修复关闭快捷标注时矩形框第二次点击完成后画布未及时刷新，并完善编辑、连续标注和绘制状态清理。
- 增加标注名称显示设置，统一控制点尺寸与绘制模式光标行为，并保持系统短十字光标显示。
- 为矩形框增加贯穿画布的纯黑实线十字辅助线，在系统短光标热点周围保留 `20px` 原始背景采样空隙，避免短光标被长线反转为浅色。
- 补充矩形框交互、光标、渲染与设置回归测试，并同步更新规格、架构、README 和代码清单。

# [1.2.2] - 2026-07-09

本版本聚焦于桌面工作台的可用性与一致性修复，重点完善了开发态与打包态的图标链路、标注页交互命名与入口组织，并继续优化首页与标注页的大目录首屏体验，同时补齐提交记录维护规则与相关文档说明。

## 本版本提交记录

## 修复图标链路并调整标注编辑入口文案（6abd1be）

* 修正 `CHANGELOG.md` 与 `AGENTS.md` 的提交记录规则：最新一次总提交不得在同一提交里回填自己的 hash，避免 amend 后 hash 反复变化的自引用问题；仅允许后续独立整理时回填已稳定存在的历史提交 hash。
* 修复 GUI 图标加载链路，统一改为通过 `src/shared/paths.py` 读取 `src/assets/app\\\_icon.png`，并在 `QApplication` 与主窗口两层同时设置图标，恢复开发态与打包态窗口/任务栏图标显示。
* 调整数据标注交互文案：主画布右键菜单的“选择”改名为“编辑”；“画标注框”弹窗顶部新增独立“编辑”入口，并与下方绘制类型分组分隔；同步更新标注规格与 README。

## 优化首页与标注页首屏性能并收紧标注转换输出规则（ef04c8f）

* 主窗口改为先显示首页、空闲后分批预热其余页面，主页概览统计/类别分布/训练曲线/训练历史改为后台汇总刷新，切回主页时保留旧统计值直至新结果返回，减少启动和切页卡顿与闪动。
* 标注页首轮图片扫描延后到页面首次显示后启动；预热阶段可提前准备首张图片与首批列表项，并继续后台分批补齐图片列表和标注状态，修复文件名重影与只读勾选框发灰问题，补充对应服务层与 UI 回归测试。
* 收紧标注转换输出目录创建规则：仅在实际写出新文件时创建目录，未开启备份时不再创建 `old/`，空 split 不再生成 `train/val/test` 目录，`data.yaml` 同步去掉空 split 条目。
* 图片压缩页新增“打开结果文件夹”，并同步更新 README、架构/规格文档、代码清单以及 AGENTS/CHANGELOG 提交流程约束说明。

## 修复开发态 CLI 根路径并预备 1.2.2 维护更新（0c3aee5）

* 同步更新 `docs/code-inventory.md`，并清理阶段性重构计划文件。
* 在项目根目录新增 `YOLOTool.lnk` 快捷方式，直接打开 `src/open\\\_yolo\\\_tool.pyw` 并复用项目程序图标。
* 修正 `CHANGELOG.md` 记录规则，改为“最新在最上”的纯记录格式，并同步约束 `AGENTS.md`。
* 修复开发态共享根路径解析错误，确保训练/验证等隐藏 CLI 子进程从仓库根目录启动，不再因 `python -m src.main` 找不到 `src` 包而直接退出。
